# Core ETL
# Versions are relaxed to >=minimum so that Airflow's constraint file can
# resolve without conflicts.  The constraint file (constraints-2.8.1) will
# pin the exact version installed inside the container.
pandas>=2.1.4
pyarrow>=14.0.2

# Socrata API client
sodapy>=2.2.0

# Retry logic with exponential backoff
tenacity>=8.2.3

# HTTP (sodapy dependency)
requests>=2.31.0

# Data Warehouse
# Airflow 2.8.x ships with SQLAlchemy 1.4.x.  Our code uses only
# create_engine(), engine.begin(), text() — all stable across 1.4→2.0.
sqlalchemy>=1.4.51
psycopg2-binary>=2.9.9

# Data Quality
great-expectations>=0.18.19,<1.0

# Fuzzy string matching (Phase 1.3 — geographic key normalisation)
# rapidfuzz is the modern, C-extension-backed replacement for thefuzz.
# It is used directly in transform_and_merge.py via `from rapidfuzz import fuzz, process`.
rapidfuzz>=3.9.3
